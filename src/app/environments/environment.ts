export const environment = {
  production: false,
  // In development, the proxy forwards requests from /api to the backend.
  apiUrl: 'http://127.0.0.1:5000',
  endpoints: {
    comparateur: '/compare'
  }
};